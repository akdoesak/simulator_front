import { Routes } from '@angular/router';
import { ElevatorListComponent } from './components/elevator-list/elevator-list.component';

export const routes: Routes = [
  { path: '', component: ElevatorListComponent }
];
